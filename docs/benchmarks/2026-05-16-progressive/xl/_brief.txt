Design a comprehensive integration plan for the FullSense umbrella covering all five live products (llmesh, llive, llove, lldesign, lltrade) plus parking candidates (llcad, lleda, llchip). The plan must address: (1) shared schema for the Approval Bus signal format that works across UI design briefs, paper-trading signals, and future CAD/EDA toolchain requests; (2) backwards-compatible MCP tool naming convention spanning current ll{name}_* tools and future tool families; (3) cross-product memory schema for the SQLite Ledger so a design DNA learned in lldesign can inform an lltrade risk policy when both products inform the same downstream RPAR axis; (4) a contradiction-resolution protocol when two product daemons request mutually exclusive Approval Bus actions in the same time window; (5) a coreogenesis mechanism by which novel domain vocabulary observed in operator briefs (e.g. a freshly-introduced regulatory term) propagates to the RAD 49-domain corpus index without manual intervention; (6) a falsification protocol where the system actively seeks counterexamples to its own Process Memory beliefs before applying them to high-stakes decisions in lltrade or the future llchip product. Use the segmentation principle to break the plan into per-axis deliverables (KAR/DTKR/APO/ICP/TLB/Math/PM/RPAR/SIL), the asymmetric distribution principle to weight the deliverables by current axis maturity (APO/TLB/SIL/ICP are partially implemented; KAR/DTKR/Math/PM/RPAR are framework-only), and the preliminary anti-action principle to flag any deliverable that would require breaking existing public API. Output as a numbered plan, max 12 items.
