Generate a YAML paper-trading strategy for the lltrade package. The strategy targets 'tech earnings dispersion' (long high-beat, short low-beat). Required YAML fields: name, universe (5 tickers from AAPL/MSFT/GOOGL/META/AMZN), regime, entry_signal, exit_signal, position_size_pct, max_drawdown_pct (must be <= 10), sharpe_target (>= 1.5), sector_caps. CRITICAL: include a top-level boolean 'paper_trading: true' (the package refuses to run without it). Output ONLY the YAML, no commentary, no fenced block.